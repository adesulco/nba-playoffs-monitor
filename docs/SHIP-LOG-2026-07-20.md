# Ship log — recovery session started 2026-07-20 (Claude Code)

Running one-paragraph reports per ticket, per the handover cadence. Newest at bottom.

## T0 — repo rescue · DONE

**Shipped:** The repo was unusable — every `git status` hung forever. Root cause was NOT iCloud: ~1,664 working-tree files, 1,304 loose git objects, and most of `packages/content-engine/.venv` carried the APFS *dataless* flag with no file provider to materialize them, so any read blocked forever (hung git processes had piled up since June). Recovery: quarantined the damaged tree to `../nba-playoffs-monitor-DAMAGED-2026-07-20/`, fresh-cloned origin (`4060d2e` = v0.80.3) to the canonical path, and salvaged every readable divergent file. **The "Jul-2 WIP" is actually the un-shipped v0.81.0 "Pick'em + Home overhaul" (Ade's 10-item directive, Jun 18–22)** — 25 tracked files + 3 new ones, including Pick'em-as-home-lead, LiveResultsStrip, EN-first i18n, light-paper theme, WC-only scope, and vitest/eslint deploy wiring. It sits on a stale pre-v0.80.3 base (an interrupted Jun-22 merge left `AUTO_MERGE` + a stale `index.lock`), so committing it to main would revert shipped work (D1 sponsor_pool, D5 entriesCsv); `api/_lib/pickem/league-settings.js` was dataless and its WIP content is unrecoverable. Decision: **preserved byte-faithfully on branch `wip/2026-06-18-home-overhaul-v0.81` (pushed), not merged** — cherry-pick source for R0-4/R1/R2; of the 3 files the handover named, league-detail/config turned out identical to HEAD. Doc bundles committed to main (`docs/handover-2026-07-18/`, `docs/pickem-flagship/`, `design_handoff_gibol_redesign/`, `design_handoff_flagship/` minus its one dataless file). CLAUDE.md (repo + umbrella) reconciled: kamu-register EN-default voice, 12/12 function count, Polymarket removed, fonts amendment, read-first → handover package. Salvaged untracked keepers (`.env.local`, `.env.production.local`, `.vercel/`) copied over; `packages/content-engine/.env` was dataless (lost — content-engine local env needs re-creating from `.env.example` when Phase 1 resumes).

**Verified:** `git status` instant; main = `b3024a9` pushed; prod 200 OK (docs-only push, still v0.80.3).

**Next:** R0-1 WC2026 backfill + score (overdue — final was Jul 19).

## R0 — recovery & data spine · COMPLETE (v0.81.0 → v0.81.1)

**Shipped.** R0-1: all 104 WC2026 fixtures backfilled and scored, including the Jul-19 final (Spain 1–0 Argentina, AET) — the Jun-12→Jul-20 blackout is closed, via `wc-backfill.yml` (workflow_dispatch, green). R0-2: `scripts/backfill-fixtures-football.mjs` is one generic football backfill+score engine (competition registry, deterministic provider-agnostic UUIDs, `lock_at` never drifts on matched rows, KO outcome = the advancing side with penalty-shootout tiebreak) behind `football-backfill.yml` every 2h with a `--skip-if-idle` cheap-exit. R0-3: EPL 2026/27 seeded, 380 fixtures across 38 clean matchweeks. R0-3b: AFF 2026 seeded and live. R0-4: the 105-test Vitest suite now gates deploys. R0-5: function budget verified at exactly 12/12. R0-6: the AFF→EPL rollover banner. R0-7: a scoring-liveness alarm in `/api/health/data-sources`.

**Two source discoveries changed the plan.** The **API-Football plan has lapsed to free** ("Free plans do not have access to this season") — it was the assumed data path for every one of these tickets. Everything was re-pointed to ESPN's public soccer API (WC + AFF, verified complete: 104 and 26 events, real tricodes, shootout data) and fixturedownload (EPL season seed with round numbers). Renewal is Ade's; nothing is blocked meanwhile, and the api-football source stays wired for its return. Second: **EPL MW1 is Aug 21–24, not Aug 15** — both independent sources agree, so R3's freeze and root-flip dates move a week later than the handover assumed.

**Three judgement calls worth knowing.** (1) The 3rd-place match ships as stage `'3rd'`, deviating from the A8 map's `'SF'`: `pickem_score_bracket` validates `sf_winner` picks against *any* final SF fixture, so a 3rd-place winner — a team that lost its semi — would have earned phantom bracket points. (2) The AFF semis and final are two-legged, so they ship as leg-suffixed stages (`SF-L1`, `SF-L2`, `F-L1`, `F-L2`), invisible to bracket scoring, because a single leg's outcome is not the aggregate advancer; AFF is match-predictions only. (3) R0-4's gate went into the `build` script rather than `deploy.yml` alone — that workflow has failed on **every** push since June (its `VERCEL_TOKEN` is unset) while Vercel's own git integration deploys regardless, so a test step there would have gated nothing. Vercel runs `npm run build`, so a red suite now actually stops a ship. That broken workflow is worth fixing or deleting.

**Verified.** Prod on v0.81.1. WC2026: 104/104 final, 0 missing scores. AFF2026: live and **self-scoring** — two matchday-1 results (Cambodia 2–1 Singapore, Timor-Leste 0–7 Vietnam) landed with no human involved, which is the whole point of the structural fix. EPL: 380 fixtures, 38 matchweeks, MW1 Aug 21. Cron: **31/31 scheduled runs green Jul 22→25**. Suite 105/105. The liveness alarm earned its keep on day one by going red on 5 NBA "if necessary" games ESPN had scheduled for series that ended early — never finalizable, so the stale window is now bounded to 14 days and older rows report separately as `abandonedFixtures`.

**R0 exit gate: PASSED — R2 is unblocked.** Deferred deliberately: the og-derby→og-recap consolidation (its only beneficiary is `billing.js` in R5; refactoring live share surfaces mid-AFF-launch is risk without near-term payoff — do it in the Aug 25–Sep 1 quiet week).

**Next:** R1 Sistem 4a foundation (fonts → tokens+aliases → logo → 6 primitives on a flagged `/dev/primitives` route → skins → icons → theme engine → kamu-copy pass).

## ⚠ Infrastructure warning — do not work from `~/Documents`

The repo froze twice during this session, and the second time cost real work. This Mac is **actively evicting file data** under `~/Documents/Claude.nosync/`: `fileproviderd` and `bird` are in a thrash-loop (188% and 99% CPU), the data volume is at **91% capacity**, and files acquire the APFS *dataless* flag with no file provider able to rehydrate them — so any read blocks forever and `git status` hangs. Note `.nosync` in the path did not prevent it. Files that were readable at the start of the session were unreadable an hour later, including `.env.production.fresh` and `.env.production.local` (both lost; `.env.local` was salvaged and carries the service-role key and admin token).

**The working clone now lives at `~/gibol-workspace/nba-playoffs-monitor`** and everything since v0.81.0 was committed from there. The frozen tree is quarantined at `~/Documents/.../nba-playoffs-monitor-DAMAGED-2026-07-20.noindex` — it has no unique content left, so it can be deleted to reclaim space, which would also help the disk-pressure problem. Freeing space and letting iCloud settle is worth doing before the next session.

