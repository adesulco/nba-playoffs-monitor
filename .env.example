# Google Analytics 4 Measurement ID (e.g. G-XXXXXXXXXX).
# Set this in Vercel → Project → Settings → Environment Variables → Production.
# Safe to leave empty in local dev — analytics is a no-op when unset.
VITE_GA_ID=
